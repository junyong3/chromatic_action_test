{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    // be explicit about types included
    // to avoid clashing with Jest types
    "types": ["cypress", "cypress-localstorage-commands"]
  },
  "exclude": ["../node_modules/@types","../cypress.config.ts"],
  "include": [
    "../node_modules/cypress",
    "./**/*.ts"
  ]
}
